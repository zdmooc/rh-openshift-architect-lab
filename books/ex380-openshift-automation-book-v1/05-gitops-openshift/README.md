
# 05 – GitOps avec OpenShift GitOps (Argo CD)

Objectifs :

- Installer et utiliser l’Operator OpenShift GitOps.
- Gérer un namespace infra via des `Application` Argo CD.
- Gérer un namespace applicatif (app déployée depuis un repo Git).
- Introduire un pattern simple d’App-of-Apps.

