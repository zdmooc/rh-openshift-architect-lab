# Lab 07 – Object Storage : OBC & s3cmd

Objectif : créer un ObjectBucketClaim, récupérer les credentials S3 et utiliser
`s3cmd` pour écrire/lire dans le bucket.

Fichiers :
- `obc-sample.yaml` : exemple d'ObjectBucketClaim.
- `s3cmd-example.conf` : modèle de configuration s3cmd.
