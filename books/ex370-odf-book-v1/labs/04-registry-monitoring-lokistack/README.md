# Lab 04 – Registry, Monitoring et LokiStack sur ODF

Objectif : faire pointer la registry interne, le monitoring et éventuellement
LokiStack sur ODF.

Fichiers :
- `registry-config-patch.yaml` : patch de la config registry.
- `monitoring-storage-patch.yaml` : exemple pour monitoring sur ODF.
- `lokistack-values-example.yaml` : exemple de valeurs pour LokiStack.
